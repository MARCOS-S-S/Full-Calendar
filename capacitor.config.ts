import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcossilqueira.fullcalendar',
  appName: 'Full-Calendar',
  webDir: 'dist'
};

export default config;
